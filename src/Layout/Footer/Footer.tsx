import './Footer.sass';

const Footer = () => (
    <section className={"footer"}>
        <div className="container">
            Footer Section
        </div>
    </section>
);

export default Footer;
