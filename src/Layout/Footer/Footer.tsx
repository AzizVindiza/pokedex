import './Footer.sass';

const Footer = () => (
    <footer className={"footer"}>
        <div className="container">
            Footer Section
        </div>
    </footer>
);

export default Footer;
