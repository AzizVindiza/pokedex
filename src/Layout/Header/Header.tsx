import './Header.sass';

const Header = () => (
    <section className={"header"}>
        <div className="container">
            Header Section
        </div>
    </section>
);

export default Header;
