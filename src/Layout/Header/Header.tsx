import './Header.sass';

const Header = () => (
    <header className={"header"}>
        <div className="container">
            Header Section
        </div>
    </header>
);

export default Header;
